
//variabel init
var number = 4
    //evaluasi condition
    while (number <= 10) {

        if (number % 2 == 0) {

            console.log(`bilangan ${number} adalah bilangan GENAP`);

        } else {
            console.log(`bilangan ${number} adalah bilangan GANJIL`);

        }
    //setelah menjalankan fungsi if di atas     
        number ++
    }

    // cek after looping
console.log(`looping selesai`);