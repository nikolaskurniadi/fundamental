var input = prompt("Silakan masukkan angka berapa pun")
input = parseInt(input)

var hasil = Math.pow((input),2)

var result = `kuadrat dari ${input} = ${hasil}`

    console.log(result);
    alert(result)